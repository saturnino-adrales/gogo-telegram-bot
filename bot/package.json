{
  "name": "telegram-bot-skill",
  "version": "1.0.0",
  "description": "Claude Code Telegram bot skill — ephemeral bot with SDK session bridge",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.0",
    "js-yaml": "^4.1.0",
    "telegraf": "^4.16.0"
  },
  "devDependencies": {
    "vitest": "^3.1.0"
  }
}
